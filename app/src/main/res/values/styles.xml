<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">#00be2c</item>
    </style>

    <style name="ResetButton" parent="Theme.AppCompat.Light">
        <item name="colorButtonNormal">#d9000b</item>
        <item name="android:textColor">#FFF</item>
        <item name="android:textAllCaps">true</item>
    </style>

    <style name="AgendarButton" parent="Theme.AppCompat.Light">
        <item name="colorButtonNormal">#00be2c</item>
        <item name="android:textColor">#FFF</item>
        <item name="android:textAllCaps">true</item>
    </style>

    <style name="TextAlign" parent="Theme.AppCompat.Light">
        <item name="android:textAlignment">center</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

</resources>
